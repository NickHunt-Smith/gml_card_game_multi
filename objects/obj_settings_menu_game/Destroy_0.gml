if exit_game_button != noone {
	instance_destroy(exit_game_button);
	exit_game_button = noone;
}

if volume_button != noone {
	instance_destroy(volume_button);
	volume_button = noone;
}

if main_menu_button != noone {
	instance_destroy(main_menu_button);
	main_menu_button = noone;
}